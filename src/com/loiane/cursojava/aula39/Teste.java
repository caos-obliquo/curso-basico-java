package com.loiane.cursojava.aula39;

public class Teste {
    // O 'public' permite que todos os pacotes tenham acesso aos métodos, construtores e atributos.
    // O 'default' permite que apenas tenha acesso para o pacote em que ele está inserido.
    // O 'private' só pode ser acessado através de herança.
    // O 'protected' permite acesso a sublcasses, mesmo que estejam em outros pacotes.
}
